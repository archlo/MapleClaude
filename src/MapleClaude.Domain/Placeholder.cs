namespace MapleClaude.Domain;

/// <summary>
/// Phase 1 placeholder so the assembly compiles. Replaced by concrete POD types
/// (Account, ClientKey, WorldInfo, ChannelInfo, AvatarLook, CharacterEntry) in
/// the phase-1/login-stage and phase-1/char-select branches.
/// </summary>
public static class Placeholder
{
    public const string Phase = "phase-1/scaffolding";
}
